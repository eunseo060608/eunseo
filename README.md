# eunseo

## 카드 매칭 게임 (Netlify)

- 게임 파일: `wtma/card-matching-game.html`
- Netlify **Publish directory**는 `wtma` 로 설정 (`netlify.toml`에 포함됨)
- 사이트 주소 루트(`/`) → `index.html`이 게임으로 연결됩니다

로컬에서 테스트: `wtma/card-matching-game.html` 을 브라우저로 열기
